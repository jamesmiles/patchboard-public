---
title: Fix Broken Tests
---

PR#{{PR_NUMBER}} ("{{TITLE}}") on branch `{{HEAD_REF}}` → `{{BASE_REF}}` has failing CI checks.

Please fix the failing tests following the guidelines in your role definition. After pushing your fix, monitor CI with `gh pr checks {{PR_NUMBER}} --watch` and repeat until all checks pass.
